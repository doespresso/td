<?php

/**
 * Здесь указываются дополнительные условия выборки элементов модуля. 
 * Действует только со стороны CMS.
 * Ключами массива являются имена таблиц в БД, а значениями - строка с условиями выборки.
 * Строка условий обязательно должна начинаться с AND или OR
 * Пример:
 * $GLOBALS['sb_dop_where'] = array(
 *   'sb_plugins_1'  =>  'AND user_f_18 >= 0'
 * );
 */

?>
